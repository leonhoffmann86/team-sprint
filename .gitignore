# local scratch
*.log
.DS_Store

# local editor / machine-specific (never commit — leaks paths, accumulates session cruft)
.vscode/
.idea/
.hermes/

# Claude Code: tracked project settings stay out (they accumulate machine-specific
# "always allow" grants); per-session grants belong in the gitignored .local file.
.claude/settings.json
.claude/settings.local.json

# Sprint runtime sidecars (also gitignored in target repos by bootstrap)
TODO.autoplan.md
TODO.review.md
TODO.run.log
.sprint-state/
.brief/
