---
name: bootstrap
description: Install the LHTask autonomous TODO workflow into the current repository — git hooks, lhtask.conf, starter constitution/lifecycle files. Run once per repo; idempotent.
argument-hint: (none — run inside the target repo)
---

Invoke the `lhtask:bootstrap` skill via the Skill tool and follow it exactly. Pass through any arguments: $ARGUMENTS
