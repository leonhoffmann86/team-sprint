---
description: Re-sync the vendored LHTask chain (stage scripts, subagents, MCP config, git hooks) in the current repo — or all registered repos with --all. Leaves lhtask.conf and lifecycle files untouched.
argument-hint: "[--all]   (no arg = current repo; --all = every registered repo)"
---

Invoke the `lhtask:update` skill via the Skill tool and follow it exactly. Pass through any arguments: $ARGUMENTS
